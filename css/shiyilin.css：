/* ===== 拾忆林容器 ===== */
.shiyilin-app {
    width: 100%;
    height: 100%;
    background: #f5f0e8;
    display: flex;
    flex-direction: column;
    font-family: "STKaiti", "KaiTi", "楷体", "Noto Serif SC", serif;
    overflow: hidden;
}

/* ===== 顶部栏 ===== */
.sl-top-bar {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 48px 16px 12px;
    background: rgba(245, 240, 232, 0.95);
    backdrop-filter: blur(10px);
    -webkit-backdrop-filter: blur(10px);
    z-index: 50;
    flex-shrink: 0;
    border-bottom: 1px solid rgba(139, 119, 90, 0.2);
}
.sl-back-btn {
    color: #6b5a4b;
    font-size: 24px;
    cursor: pointer;
    width: 36px;
    height: 36px;
    display: flex;
    align-items: center;
    justify-content: center;
    font-weight: 300;
}
.sl-title {
    font-size: 18px;
    color: #4a3728;
    font-weight: 500;
    letter-spacing: 4px;
}

/* ===== 书架区域 ===== */
.sl-shelf {
    flex: 1;
    overflow-y: auto;
    padding: 20px 16px 40px;
    -webkit-overflow-scrolling: touch;
}

/* ===== 书架行 ===== */
.sl-row {
    display: flex;
    gap: 16px;
    margin-bottom: 24px;
}

/* ===== 单本书 ===== */
.sl-book {
    flex: 1;
    max-width: calc(50% - 8px);
    cursor: pointer;
    transition: transform 0.2s;
}
.sl-book:active {
    transform: scale(0.96);
}

/* ===== 书封（黑色硬皮） ===== */
.sl-book-cover {
    width: 100%;
    aspect-ratio: 3 / 4;
    background: linear-gradient(160deg, #1a1a1a 0%, #111 30%, #0f0f0f 60%, #1a1a1a 100%);
    border-radius: 4px 12px 12px 4px;
    box-shadow: 4px 4px 16px rgba(0,0,0,0.4), inset 2px 0 6px rgba(255,255,255,0.02);
    position: relative;
    overflow: hidden;
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
}

/* 书封装饰线 */
.sl-book-cover::before {
    content: '';
    position: absolute;
    top: 12px;
    left: 12px;
    right: 12px;
    bottom: 12px;
    border: 1px solid rgba(255, 255, 255, 0.06);
    border-radius: 2px 8px 8px 2px;
    pointer-events: none;
}

/* 书脊活页孔 */
.sl-book-cover::after {
    content: '';
    position: absolute;
    left: 8px;
    top: 50%;
    transform: translateY(-50%);
    width: 6px;
    height: 60%;
    background: repeating-linear-gradient(0deg, transparent, transparent 30px, rgba(255,255,255,0.04) 30px, rgba(255,255,255,0.04) 42px);
    border-radius: 3px;
    pointer-events: none;
}

/* 书封标题 */
.sl-cover-title {
    font-size: 15px;
    color: #ccc;
    letter-spacing: 3px;
    font-weight: 300;
    text-align: center;
    padding: 0 16px;
}
.sl-cover-subtitle {
    font-size: 10px;
    color: rgba(255,255,255,0.2);
    letter-spacing: 2px;
    margin-top: 6px;
}

/* ===== 书脊标签 ===== */
.sl-book-spine {
    margin-top: 6px;
    text-align: center;
}
.sl-spine-name {
    font-size: 11px;
    color: #4a3728;
    letter-spacing: 2px;
    font-weight: 500;
}

/* ===== 打开的书（全屏） ===== */
.sl-book-open {
    position: fixed;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background: rgba(0,0,0,0.7);
    z-index: 300;
    display: flex;
    align-items: center;
    justify-content: center;
}

/* ===== 放大后的书本容器 ===== */
.sl-book-viewer {
    width: 92%;
    max-width: 420px;
    height: 85%;
    perspective: 1200px;
    position: relative;
}

/* ===== 封面（正面朝上，可翻开） ===== */
.sl-viewer-cover {
    position: absolute;
    width: 100%;
    height: 100%;
    background: linear-gradient(160deg, #1a1a1a 0%, #111 30%, #0f0f0f 60%, #1a1a1a 100%);
    border-radius: 4px 16px 16px 4px;
    box-shadow: 8px 8px 30px rgba(0,0,0,0.5);
    z-index: 10;
    transform-origin: left center;
    transition: transform 0.8s cubic-bezier(0.25, 0.46, 0.45, 0.94);
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    cursor: pointer;
}
.sl-viewer-cover.open {
    transform: rotateY(-150deg);
    box-shadow: -12px 8px 30px rgba(0,0,0,0.6);
}

/* 封面装饰 */
.sl-viewer-cover::before {
    content: '';
    position: absolute;
    top: 20px;
    left: 20px;
    right: 20px;
    bottom: 20px;
    border: 1px solid rgba(255,255,255,0.06);
    border-radius: 2px 12px 12px 2px;
    pointer-events: none;
}

/* 封面文字 */
.sl-viewer-cover-title {
    font-size: 22px;
    color: #ccc;
    letter-spacing: 5px;
    font-weight: 300;
}
.sl-viewer-cover-subtitle {
    font-size: 12px;
    color: rgba(255,255,255,0.2);
    letter-spacing: 3px;
    margin-top: 10px;
}
.sl-viewer-cover-hint {
    position: absolute;
    bottom: 30px;
    font-size: 11px;
    color: rgba(255,255,255,0.15);
    letter-spacing: 2px;
}

/* ===== 内页面板（牛皮纸底纹） ===== */
.sl-pages-panel {
    position: absolute;
    width: 100%;
    height: 100%;
    background: #faf3e6;
    border-radius: 4px 16px 16px 4px;
    box-shadow: 8px 8px 30px rgba(0,0,0,0.5), inset 0 0 80px rgba(139, 119, 90, 0.1);
    display: flex;
    flex-direction: column;
    overflow: hidden;
    z-index: 5;
}

/* 牛皮纸纹理 */
.sl-pages-panel::before {
    content: '';
    position: absolute;
    top: 0; left: 0; right: 0; bottom: 0;
    background: 
        radial-gradient(ellipse at 20% 80%, rgba(139, 119, 90, 0.08) 0%, transparent 50%),
        radial-gradient(ellipse at 80% 20%, rgba(160, 140, 100, 0.06) 0%, transparent 50%);
    pointer-events: none;
    z-index: 0;
}

/* 活页孔 */
.sl-page-holes {
    position: absolute;
    left: 14px;
    top: 50%;
    transform: translateY(-50%);
    width: 8px;
    height: 70%;
    display: flex;
    flex-direction: column;
    justify-content: space-around;
    z-index: 5;
    pointer-events: none;
}
.sl-page-hole {
    width: 8px;
    height: 22px;
    border: 2px solid #8b775a;
    border-radius: 4px;
    background: #faf3e6;
}

/* 内页顶部栏 */
.sl-pages-top {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 16px 20px 8px 32px;
    border-bottom: 1px dashed rgba(139, 119, 90, 0.2);
    flex-shrink: 0;
    z-index: 1;
}
.sl-pages-close {
    font-size: 20px;
    color: #8b775a;
    cursor: pointer;
    padding: 4px 8px;
}
.sl-pages-title {
    font-size: 15px;
    color: #4a3728;
    letter-spacing: 3px;
}

/* 内页内容区 */
.sl-pages-body {
    flex: 1;
    overflow-y: auto;
    padding: 20px 20px 30px 32px;
    z-index: 1;
    -webkit-overflow-scrolling: touch;
    position: relative;
}

/* 内页横线 */
.sl-pages-body::before {
    content: '';
    position: absolute;
    top: 0; left: 0; right: 0; bottom: 0;
    background: repeating-linear-gradient(0deg, transparent, transparent 27px, rgba(139, 119, 90, 0.12) 27px, rgba(139, 119, 90, 0.12) 28px);
    pointer-events: none;
    z-index: 0;
    padding-top: 20px;
}

/* 装饰植物图案 */
.sl-page-decoration {
    position: absolute;
    pointer-events: none;
    z-index: 0;
    opacity: 0.04;
    font-size: 120px;
    color: #4a3728;
}
.sl-page-decoration.top-left {
    top: 10px;
    left: 10px;
}
.sl-page-decoration.bottom-right {
    bottom: 10px;
    right: 10px;
    transform: rotate(180deg);
}

/* 总结文字 */
.sl-summary-text {
    position: relative;
    z-index: 1;
    font-size: 15px;
    line-height: 2;
    color: #3a2a1a;
    text-indent: 2em;
    word-break: break-word;
    white-space: pre-wrap;
}

/* 空状态 */
.sl-empty {
    text-align: center;
    color: #b0a08a;
    padding: 80px 20px;
    font-size: 14px;
    letter-spacing: 2px;
}
